{{#each modalInstances as |modalInstance|}}
    {{modal-instance 
        modalInstance.name
        modalInstance.options
        modalInstance.uuid
        on-close=(action 'modalClosed' modalInstance)}}
{{/each}}
