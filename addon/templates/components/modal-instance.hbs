{{#if isModal}}
    {{component modalName
        modalOptions=options
        autoOpen=true
        on-close=on-close
    }}
{{else}}
    {{#dialog-wrapper on-close=on-close autoOpen=true}}
        {{component modalName
            modalOptions=options
        }}
    {{/dialog-wrapper}}
{{/if}}